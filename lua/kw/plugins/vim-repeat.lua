return {
    "kana/vim-repeat",
    config = function()
        -- Configuration for vim-repeat
        -- The plugin automatically allows repeating supported plugin actions with the '.' key
    end,
}
